{
  "name": "community-services",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && uvicorn app.all_endpoints:socket_app"
  }
}

